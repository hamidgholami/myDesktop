all:
  children:
       myUbuntu:
          vars:
            ansible_user: vagrant
            ansible_become: yes
          hosts:
            ubuntu:
              ansible_host: 192.168.121.178 # or localhost
              ubuntu_user: vagrant
              git_config_username:
              git_config_email:
